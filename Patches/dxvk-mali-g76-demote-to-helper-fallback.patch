diff --git a/src/d3d9/d3d9_fixed_function.cpp b/src/d3d9/d3d9_fixed_function.cpp
index b1427bf..55c9101 100644
--- a/src/d3d9/d3d9_fixed_function.cpp
+++ b/src/d3d9/d3d9_fixed_function.cpp
@@ -12,10 +12,14 @@
 
 namespace dxvk {
 
-  D3D9FixedFunctionOptions::D3D9FixedFunctionOptions(const D3D9Options* options) {
+  D3D9FixedFunctionOptions::D3D9FixedFunctionOptions(const D3D9Options* options, const Rc<DxvkDevice>& device) {
     invariantPosition = options->invariantPosition;
     forceSampleRateShading = options->forceSampleRateShading;
     drefScaling = options->drefScaling;
+
+    supportsDemoteToHelperInvocation =
+      device->features().vk13.shaderDemoteToHelperInvocation
+      || device->features().extShaderDemoteToHelperInvocation.shaderDemoteToHelperInvocation;
   }
 
   uint32_t DoFixedFunctionFog(D3D9ShaderSpecConstantManager& spec, SpirvModule& spvModule, const D3D9FogContext& fogCtx) {
@@ -191,7 +195,7 @@ namespace dxvk {
   }
 
 
-  void DoFixedFunctionAlphaTest(SpirvModule& spvModule, const D3D9AlphaTestContext& ctx) {
+  void DoFixedFunctionAlphaTest(SpirvModule& spvModule, const D3D9AlphaTestContext& ctx, bool supportsDemote) {
     // Labels for the alpha test
     std::array<SpirvSwitchCaseLabel, 8> atestCaseLabels = {{
       { uint32_t(VK_COMPARE_OP_NEVER),            spvModule.allocateId() },
@@ -325,8 +329,21 @@ namespace dxvk {
     spvModule.opBranchConditional(atestDiscard, atestDiscardLabel, atestKeepLabel);
 
     spvModule.opLabel(atestDiscardLabel);
-    spvModule.opDemoteToHelperInvocation();
-    spvModule.opBranch(atestKeepLabel);
+
+    // GeneralsX @bugfix Android port 30/07/2026 demote-to-helper keeps the
+    // invocation alive (as a "helper") so later code in the same shader
+    // still has correct derivatives; a plain kill terminates it outright.
+    // This is only safe here because alpha test is always the very last
+    // thing emitted in this pixel shader (see setupPS()/compilePS() and
+    // DxsoCompiler::emitPsProcessing) -- nothing downstream depends on
+    // this invocation surviving. OpKill is a block terminator, so unlike
+    // demote it must not be followed by a branch to the merge block.
+    if (supportsDemote) {
+      spvModule.opDemoteToHelperInvocation();
+      spvModule.opBranch(atestKeepLabel);
+    } else {
+      spvModule.opKill();
+    }
 
     // end if (do_discard)
     spvModule.opLabel(atestKeepLabel);
@@ -2216,8 +2233,13 @@ namespace dxvk {
     m_specUbo = SetupSpecUBO(m_module, m_bindings);
 
     // PS Caps
-    m_module.enableExtension("SPV_EXT_demote_to_helper_invocation");
-    m_module.enableCapability(spv::CapabilityDemoteToHelperInvocationEXT);
+    // GeneralsX @bugfix Android port 30/07/2026 unconditional regardless
+    // of device support -- see DoFixedFunctionAlphaTest/
+    // D3D9FixedFunctionOptions::supportsDemoteToHelperInvocation.
+    if (m_options.supportsDemoteToHelperInvocation) {
+      m_module.enableExtension("SPV_EXT_demote_to_helper_invocation");
+      m_module.enableCapability(spv::CapabilityDemoteToHelperInvocationEXT);
+    }
     m_module.enableCapability(spv::CapabilityDerivativeControl);
 
     m_module.setExecutionMode(m_entryPointId,
@@ -2468,7 +2490,7 @@ namespace dxvk {
       m_module.opLoad(m_vec4Type, oC0),
       1, &alphaComponentId);
 
-    DoFixedFunctionAlphaTest(m_module, alphaTestContext);
+    DoFixedFunctionAlphaTest(m_module, alphaTestContext, m_options.supportsDemoteToHelperInvocation);
   }
 
 
@@ -2515,7 +2537,7 @@ namespace dxvk {
     D3D9FFShaderCompiler compiler(
       pDevice->GetDXVKDevice(),
       Key, name,
-      pDevice->GetOptions());
+      D3D9FixedFunctionOptions(pDevice->GetOptions(), pDevice->GetDXVKDevice()));
 
     m_shader = compiler.compile();
     m_isgn   = compiler.isgn();
@@ -2538,7 +2560,7 @@ namespace dxvk {
     D3D9FFShaderCompiler compiler(
       pDevice->GetDXVKDevice(),
       Key, name,
-      pDevice->GetOptions());
+      D3D9FixedFunctionOptions(pDevice->GetOptions(), pDevice->GetDXVKDevice()));
 
     m_shader = compiler.compile();
     m_isgn   = compiler.isgn();
diff --git a/src/d3d9/d3d9_fixed_function.h b/src/d3d9/d3d9_fixed_function.h
index 85432ce..153c650 100644
--- a/src/d3d9/d3d9_fixed_function.h
+++ b/src/d3d9/d3d9_fixed_function.h
@@ -13,6 +13,7 @@
 namespace dxvk {
 
   class D3D9DeviceEx;
+  class DxvkDevice;
   class SpirvModule;
 
   struct D3D9Options;
@@ -45,11 +46,23 @@ namespace dxvk {
   };
 
   struct D3D9FixedFunctionOptions {
-    D3D9FixedFunctionOptions(const D3D9Options* options);
+    D3D9FixedFunctionOptions(const D3D9Options* options, const Rc<DxvkDevice>& device);
 
     bool    invariantPosition;
     bool    forceSampleRateShading;
     int32_t drefScaling;
+
+    // GeneralsX @bugfix Android port 30/07/2026 see setupPS()/alphaTestPS()
+    // (d3d9_fixed_function.cpp) and DoFixedFunctionAlphaTest -- whether
+    // the device supports VK_EXT_shader_demote_to_helper_invocation/
+    // Vulkan 1.3's shaderDemoteToHelperInvocation, unconditionally
+    // assumed true before (VUID-VkShaderModuleCreateInfo-pCode-08740 on
+    // Mali-G76). Populated here (rather than as a D3D9FFShaderCompiler
+    // member, computed from the Rc<DxvkDevice> already passed to its
+    // constructor) purely to avoid inserting new lines next to
+    // dxvk-mali-g76-extended-dynamic-state.patch's own edits to that
+    // class, which sit right next to where such a member would go.
+    bool    supportsDemoteToHelperInvocation;
   };
 
   constexpr float GetDrefScaleFactor(int32_t bitDepth) {
@@ -60,7 +73,11 @@ namespace dxvk {
   // Returns new oColor if PS
   uint32_t DoFixedFunctionFog(D3D9ShaderSpecConstantManager& spec, SpirvModule& spvModule, const D3D9FogContext& fogCtx);
 
-  void DoFixedFunctionAlphaTest(SpirvModule& spvModule, const D3D9AlphaTestContext& ctx);
+  // GeneralsX @bugfix Android port 30/07/2026 supportsDemote: whether the
+  // device supports VK_EXT_shader_demote_to_helper_invocation/Vulkan 1.3.
+  // If not, falls back to a plain discard (SpirvModule::opKill) instead
+  // of demoting the invocation -- see the .cpp for why this is safe here.
+  void DoFixedFunctionAlphaTest(SpirvModule& spvModule, const D3D9AlphaTestContext& ctx, bool supportsDemote);
 
   // Returns a render state block
   uint32_t SetupRenderStateBlock(SpirvModule& spvModule);
diff --git a/src/dxso/dxso_compiler.cpp b/src/dxso/dxso_compiler.cpp
index dfaeeb2..008dc37 100644
--- a/src/dxso/dxso_compiler.cpp
+++ b/src/dxso/dxso_compiler.cpp
@@ -495,8 +495,13 @@ namespace dxvk {
 
 
   void DxsoCompiler::emitPsInit() {
-    m_module.enableExtension("SPV_EXT_demote_to_helper_invocation");
-    m_module.enableCapability(spv::CapabilityDemoteToHelperInvocationEXT);
+    // GeneralsX @bugfix Android port 30/07/2026 unconditional regardless
+    // of device support -- see DoFixedFunctionAlphaTest/
+    // DxsoOptions::supportsDemoteToHelperInvocation.
+    if (m_moduleInfo.options.supportsDemoteToHelperInvocation) {
+      m_module.enableExtension("SPV_EXT_demote_to_helper_invocation");
+      m_module.enableCapability(spv::CapabilityDemoteToHelperInvocationEXT);
+    }
     m_module.enableCapability(spv::CapabilityDerivativeControl);
 
     m_module.setExecutionMode(m_entryPointId,
@@ -3114,8 +3119,21 @@ void DxsoCompiler::emitControlFlowGenericLoop(
     m_module.opBranchConditional(result, labelIf, labelEnd);
 
     m_module.opLabel(labelIf);
-    m_module.opDemoteToHelperInvocation();
-    m_module.opBranch(labelEnd);
+
+    // GeneralsX @bugfix Android port 30/07/2026 see DoFixedFunctionAlphaTest
+    // (d3d9_fixed_function.cpp) for the general rationale. Unlike alpha
+    // test, texkill can be followed by more shader code that samples
+    // textures and relies on correct derivatives, which a plain discard
+    // does not preserve as accurately as demote -- a minor filtering
+    // inaccuracy near texkill boundaries on devices without the
+    // extension, which is an acceptable tradeoff against not being able
+    // to compile the shader at all.
+    if (m_moduleInfo.options.supportsDemoteToHelperInvocation) {
+      m_module.opDemoteToHelperInvocation();
+      m_module.opBranch(labelEnd);
+    } else {
+      m_module.opKill();
+    }
 
     m_module.opLabel(labelEnd);
   }
@@ -3650,7 +3668,7 @@ void DxsoCompiler::emitControlFlowGenericLoop(
         m_module.opLoad(m_module.defVectorType(floatType, 4), oC0.id),
         1, &alphaComponentId);
 
-      DoFixedFunctionAlphaTest(m_module, alphaTestContext);
+      DoFixedFunctionAlphaTest(m_module, alphaTestContext, m_moduleInfo.options.supportsDemoteToHelperInvocation);
     }
   }
 
diff --git a/src/dxso/dxso_options.cpp b/src/dxso/dxso_options.cpp
index 24ba92e..66d530f 100644
--- a/src/dxso/dxso_options.cpp
+++ b/src/dxso/dxso_options.cpp
@@ -14,6 +14,16 @@ namespace dxvk {
     const DxvkDeviceFeatures& devFeatures = device->features();
     const DxvkDeviceInfo& devInfo = adapter->devicePropertiesExt();
 
+    // GeneralsX @bugfix Android port 30/07/2026 see DxsoOptions::
+    // supportsDemoteToHelperInvocation (dxso_options.h). Placed here,
+    // away from the drefScaling/supportsClipDistance area below, to
+    // avoid the context-window conflict that broke
+    // dxvk-mali-clip-distance.patch's own reverse-apply check when a new
+    // field was first appended right after supportsClipDistance instead.
+    supportsDemoteToHelperInvocation =
+      devFeatures.vk13.shaderDemoteToHelperInvocation
+      || devFeatures.extShaderDemoteToHelperInvocation.shaderDemoteToHelperInvocation;
+
     // Apply shader-related options
     strictConstantCopies = options.strictConstantCopies;
 
diff --git a/src/dxso/dxso_options.h b/src/dxso/dxso_options.h
index 1e390e5..a75fe12 100644
--- a/src/dxso/dxso_options.h
+++ b/src/dxso/dxso_options.h
@@ -12,6 +12,21 @@ namespace dxvk {
     DxsoOptions();
     DxsoOptions(D3D9DeviceEx* pDevice, const D3D9Options& options);
 
+    /// GeneralsX @bugfix Android port 30/07/2026 same rationale as
+    /// supportsClipDistance further below, for VK_EXT_shader_demote_to_
+    /// helper_invocation/Vulkan 1.3's shaderDemoteToHelperInvocation.
+    /// Alpha-test emulation (DoFixedFunctionAlphaTest,
+    /// d3d9_fixed_function.cpp) unconditionally used SPIR-V's
+    /// DemoteToHelperInvocation capability -- unsupported on Mali-G76
+    /// (VUID-VkShaderModuleCreateInfo-pCode-08740), which made
+    /// vkCreateShaderModule fail outright and crash downstream pipeline
+    /// creation. Falls back to a plain discard (SpirvModule::opKill)
+    /// when this is false. Placed here, away from supportsClipDistance
+    /// near the end of the struct, to avoid the context-window conflict
+    /// that broke dxvk-mali-clip-distance.patch's own reverse-apply
+    /// check when this was first appended right after it instead.
+    bool supportsDemoteToHelperInvocation;
+
     /// True:  Copy our constant set into UBO if we are relative indexing ever.
     /// False: Copy our constant set into UBO if we are relative indexing at the start of a defined constant
     /// Why?:  In theory, FXC should never generate code where this would be an issue.
diff --git a/src/spirv/spirv_module.cpp b/src/spirv/spirv_module.cpp
index 9d1c74c..0ea23d8 100644
--- a/src/spirv/spirv_module.cpp
+++ b/src/spirv/spirv_module.cpp
@@ -3728,6 +3728,12 @@ namespace dxvk {
   void SpirvModule::opDemoteToHelperInvocation() {
     m_code.putIns (spv::OpDemoteToHelperInvocation, 1);
   }
+
+
+  void SpirvModule::opKill() {
+    m_code.putIns (spv::OpKill, 1);
+    m_blockId = 0;
+  }
   
   
   void SpirvModule::opEmitVertex(
diff --git a/src/spirv/spirv_module.h b/src/spirv/spirv_module.h
index 4bfbd11..8178ad1 100644
--- a/src/spirv/spirv_module.h
+++ b/src/spirv/spirv_module.h
@@ -1264,8 +1264,13 @@ namespace dxvk {
       const SpirvPhiLabel*          sourceLabels);
     
     void opReturn();
-    
+
     void opDemoteToHelperInvocation();
+
+    // GeneralsX @bugfix Android port 30/07/2026 fallback for devices
+    // without VK_EXT_shader_demote_to_helper_invocation/Vulkan 1.3 --
+    // see DoFixedFunctionAlphaTest (d3d9_fixed_function.cpp).
+    void opKill();
     
     void opEmitVertex(
             uint32_t                streamId);
